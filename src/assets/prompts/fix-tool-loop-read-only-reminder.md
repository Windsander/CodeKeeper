你已经进行了较多只读探索，但仍未产生任何文件变更。请现在做出明确选择：

1. 如果 Reviewer 的问题已经被当前代码或已有提交修复，调用 finish({ success: false, reason: "简要说明证据" })，框架会重新判断 already-fixed。
2. 如果问题仍然存在，请先完成最小、可验证的代码修改，再运行 validate。
   不要继续只读取更多窗口、重复 validate 或只修改注释来延长循环。
